const world = document.querySelector(".world");
const nav = document.querySelector("nav");
for (let i = 0; i < 300; i++) {
  const blocks = document.createElement("div");
  world.appendChild(blocks);
}
